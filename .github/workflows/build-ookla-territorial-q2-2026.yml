name: Build Ookla territorial Q2 2026

on:
  push:
    branches: [feat/ookla-q2-2026]
    paths:
      - 'scripts/build_ookla_territorial_q2_2026.py'
      - '.github/workflows/build-ookla-territorial-q2-2026.yml'
      - 'data/ookla/chile_2026q2_fixed_tiles.csv'
      - 'data/ookla/chile_2026q2_mobile_tiles.csv'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ookla-q2-territorial
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/ookla-q2-2026
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install pandas geopandas pyogrio shapely

      - name: Build Q2 communal and regional layers
        run: |
          python scripts/build_ookla_territorial_q2_2026.py

      - name: Validate outputs
        run: |
          python - <<'PY'
          import pandas as pd
          from pathlib import Path

          comm = pd.read_csv('data/ookla/territorial/chile_2026q2_communes.csv')
          reg = pd.read_csv('data/ookla/territorial/chile_2026q2_regions.csv')
          cmpc = pd.read_csv('data/ookla/territorial/chile_2026q1_vs_2026q2_communes.csv')
          master = pd.read_csv('data/communal_master/chile_digital_inclusion_communes_2026_integrated.csv')
          coverage = pd.read_csv('data/ookla/territorial/spatial_assignment_coverage_q1_q2.csv')

          assert set(comm['period']) == {'2026Q2'}
          assert set(comm['network']) == {'fixed', 'mobile'}
          assert set(reg['period']) == {'2026Q2'}
          assert set(reg['network']) == {'fixed', 'mobile'}
          assert master['comuna'].nunique() == 346, master['comuna'].nunique()
          assert 'ookla_fixed_download_mbps_2026q2' in master.columns
          assert 'ookla_mobile_download_mbps_2026q2' in master.columns
          assert 'ookla_fixed_download_delta_pct_q1_to_q2' in master.columns
          assert 'ookla_mobile_download_delta_pct_q1_to_q2' in master.columns
          assert set(coverage['period']) == {'2026Q1', '2026Q2'}
          assert len(cmpc) > 0
          for col in ['tiles', 'tests']:
              assert (pd.to_numeric(comm[col], errors='coerce').fillna(0) > 0).all()

          print('Q2 communes:', comm.shape)
          print('Q2 regions:', reg.shape)
          print('Q1-Q2 commune comparison:', cmpc.shape)
          print('Master:', master.shape)
          print(coverage.to_string(index=False))
          PY

      - name: Commit territorial Q2 outputs
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/ookla/territorial/chile_2026q2_communes.csv \
                  data/ookla/territorial/chile_2026q2_regions.csv \
                  data/ookla/territorial/chile_2026q1_vs_2026q2_communes.csv \
                  data/ookla/territorial/chile_2026q1_vs_2026q2_regions.csv \
                  data/ookla/territorial/spatial_assignment_coverage_q1_q2.csv \
                  data/communal_master/chile_digital_inclusion_communes_2026_integrated.csv
          if git diff --cached --quiet; then
            echo 'No Q2 territorial changes to commit'
            exit 0
          fi
          git commit -m 'Add Ookla Q2 2026 territorial layers'
          git pull --rebase origin feat/ookla-q2-2026
          git push origin HEAD:feat/ookla-q2-2026
